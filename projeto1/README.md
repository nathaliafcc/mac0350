# PROJETO 1 - RANKING DE TÊNIS 

## Objetivo:
O projeto consiste em um ranking para o esporte tênis, que possui suas especificidades na hora de pontuar. Nele, será possível registrar as partidas que cada usuário jogou e, a partir desses registros, criar um ranking. No projeto inicial, não será possível fazer login, o resgistro das partidas será feito utilizando o NUSP dos dois jogadores.

## Utilidade:
O projeto visa maior organização dos status de cada usuário, dessa forma, ele pode mensurar seu desempenho e verificar se está evoluindo.

## Utilização:
Basta fazer o registro que conta com alguns dados - como idade, sexo e mão dominante - e começar a registrar as partidas jogadas.

## Como Rodar a Aplicação: 
Para executar o projeto localmente, siga os passos abaixo no seu terminal: 

1. Abra o terminal na pasta raiz do projeto. 
2. Crie um ambiente virtual executando: `python3 -m venv venv` 
3. Ative o ambiente virtual com: `source venv/bin/activate` (No Windows, use: `venv\Scripts\activate`) 
4. Instale as dependências necessárias executando: `pip install -r requirements.txt` 
5. Inicie o servidor local com o comando: `uvicorn main:app --reload` 
6. Abra o seu navegador e acesse o endereço: `http://127.0.0.1:8000` 

## Ajuda:
Quaisquer dúvidas podem ser resolvidas mandando um email para @(ainda a decidir).

## Contribuintes:
Como esse foi um projeto desenvolvido individualmente, a única contribuinte é Nathália F. C. da Costa - atualmente graduanda em Ciência da computação pela Universidade de São Paulo (USP).

## Uso de IA
Como permitido no projeto, o uso destinou-se à ajuda na implementação do front end, como ideias de design. Além disso, foi utilizado para entender alguns erros no back end.
